info:
  id: ${spring.application.name}

server:
  port: ${PORT:9999}
     
#zuul:
#  ignored-services: 
#  ignored-patterns: 
#  routes:
  
hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 60000
 
ribbon:
  ConnectTimeout: 3000
  ReadTimeout: 60000
