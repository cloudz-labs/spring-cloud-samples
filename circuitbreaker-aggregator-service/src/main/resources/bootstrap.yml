spring:
  application:
    name: circuitbreaker-aggregator-service
    
---
spring:
  profiles: cloud
  cloud:
    config:
      uri: ${vcap.services.configuration-service.credentials.uri:http://localhost:8888}

---
spring:
  profiles: default
  cloud:
    config:
      enabled: false